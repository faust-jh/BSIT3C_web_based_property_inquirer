import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#063970",
    },
    secondary: {
      main: "#F7CCAC",
    },
  },

  typography: {
    fontFamily: "Barlow",
  },
});

export default theme;
